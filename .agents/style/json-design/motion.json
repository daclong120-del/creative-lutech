{
  "$meta": {
    "domain": "motion",
    "source": "init-design/app/globals.css, init-design/components/**",
    "extractedFrom": "code",
    "purpose": "Define keyframes, transition rates, and rules of restraint regarding motion"
  },
  "keyframes": {
    "float-1": {
      "0%, 100%": { "transform": "translateY(0px)" },
      "50%": { "transform": "translateY(-8px)" }
    },
    "float-2": {
      "0%, 100%": { "transform": "translateY(0px)" },
      "50%": { "transform": "translateY(-12px)" }
    },
    "float-3": {
      "0%, 100%": { "transform": "translateY(0)" },
      "50%": { "transform": "translateY(-8px)" }
    }
  },
  "presets": {
    "animate-float-1": "float-1 4s ease-in-out infinite",
    "animate-float-2": "float-2 5s ease-in-out infinite",
    "animate-float-3": "float-3 4.5s ease-in-out infinite",
    "animate-pulse": "pulse animation for highlight tags or brand sparkles icons"
  },
  "transitions": {
    "states": {
      "hover": "transition-colors duration-150 / transition-all duration-200",
      "dropdownOpen": "animate-in fade-in slide-in-from-top-1 duration-150 / duration-100",
      "sidebarToggle": "transition-all duration-300 ease-in-out",
      "drawerOpen": "transition-[padding-right] duration-300 ease-out",
      "cardEntrance": "animate-in fade-in slide-in-from-bottom-2 duration-300"
    }
  },
  "rules": [
    {
      "name": "Restrained transitions",
      "rule": "Interactive elements (buttons, inputs, menu options) must use rapid color transitions (duration-150) rather than long, sluggish transitions to maintain a fast responsive feel.",
      "confidence": "observed",
      "evidence": "Hover styles on Header buttons, SupportGrid cards, and DropdownSelect buttons use transition-colors duration-150."
    },
    {
      "name": "Page and overlay entrances",
      "rule": "Entering pages or popup overlays must slide and fade into view subtly (slide-in-from-top-1 for dropdown menus, slide-in-from-bottom-2 for card boxes) rather than rotating or bouncing.",
      "confidence": "observed",
      "evidence": "Observed on DropdownSelect options opening, user profile settings list, and BrowseByTopic box loading."
    },
    {
      "name": "Slow floating decorations",
      "rule": "Restrict dynamic keyframe movements (such as float or scroll animations) strictly to illustration graphics, and ensure they run at slow rates (4s to 5s) to avoid distracting the operator.",
      "confidence": "observed",
      "evidence": "float-1, float-2, and float-3 keyframe declarations are bound under globals.css and used for illustration/background details."
    }
  ]
}
