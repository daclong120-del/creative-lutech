{
  "$meta": {
    "domain": "radius-elevation",
    "source": "init-design/app/globals.css, init-design/components/**",
    "extractedFrom": "code",
    "purpose": "Define border-radius mappings, shadow details, and design rules"
  },
  "radius": {
    "base": "0.625rem (10px)",
    "scale": {
      "sm": "calc(var(--radius) * 0.6) = 6px",
      "md": "calc(var(--radius) * 0.8) = 8px",
      "lg": "var(--radius) = 10px",
      "xl": "calc(var(--radius) * 1.4) = 14px",
      "2xl": "calc(var(--radius) * 1.8) = 18px",
      "3xl": "calc(var(--radius) * 2.2) = 22px",
      "4xl": "calc(var(--radius) * 2.6) = 26px"
    },
    "mappings": {
      "buttons": "rounded-lg (10px), rounded-md (8px), or dynamic rounded-[min(var(--radius-md),12px)] / rounded-[min(var(--radius-md),10px)]",
      "cards": "rounded-xl (14px)",
      "dropdowns": "rounded-lg (10px)",
      "outerBoxes": "rounded-2xl (18px)",
      "statusPills": "rounded-full (9999px)"
    }
  },
  "elevation": {
    "shadowless": false,
    "scale": {
      "flat": "border border-border bg-card",
      "shadow-sm": "shadow-sm (used for support cards, buttons, dropdown selects)",
      "shadow-md": "hover:shadow-md (used for support card hover highlight states)",
      "shadow-lg": "shadow-lg (used for expanded user profile settings and submenus)"
    }
  },
  "rules": [
    {
      "name": "Flat surfaces with clean borders",
      "rule": "Avoid adding drop shadows on static layouts. Card panels and dividers must sit flat and rely on border-border 1px borders for structure.",
      "confidence": "observed",
      "evidence": "SeeWhatsNew, BrowseByTopic, and support center dashed boxes use flat bg-card/bg-transparent layout wrappers with border-border."
    },
    {
      "name": "Floating popup overlays",
      "rule": "Only floating overlays (like profile submenus, dropdown options, and listboxes) should use shadow-lg or ring-1 ring-black/5 to indicate elevation over other elements.",
      "confidence": "observed",
      "evidence": "Observed on DropdownSelect options and Header user dropdown menus."
    },
    {
      "name": "Rounded hierarchy",
      "rule": "Apply corner roundness hierarchically: small buttons and dropdowns use rounded-lg (10px), larger items like cards use rounded-xl (14px), and main section/dashboard wrappers use rounded-2xl (18px).",
      "confidence": "observed",
      "evidence": "Observed across components: Button uses rounded-lg, SupportGrid uses rounded-xl, BrowseByTopic and Welcome wrapper box use rounded-2xl."
    }
  ]
}
